import styled from 'styled-components';
import colors from '../../styles/colors';

export const BottomLine = styled.div`
  width: 50px;
  height: 5px;
  background-color: ${colors.blue_line};
  margin-top: -20px;
  margin-bottom: 20px;
`;

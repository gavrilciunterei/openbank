export type TextProps = {
  children: React.ReactNode;
  color?: string;
  fontWeight?: number;
  size?: string;
  maxWidth?: string;
};

export type StyeProps = {
  color?: string;
  fontWeight?: number;
  size?: string;
  maxWidth?: string;
};
